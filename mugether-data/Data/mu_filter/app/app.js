const mu = require('../mudata_latest.json');
const hotel = require('../HotelData.json');
const resturant = require('../resturant.json');
const travel = require('../Travel.json')

console.log(travel.length);
console.log(resturant.length);
console.log(hotel.length);
